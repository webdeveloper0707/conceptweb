import React from 'react'
import { useRouter } from 'next/navigation'

function slug() {
  alert("ok")
    const router = useRouter()
    console.log(router)
  return (
    <div>
      
    </div>
  )
}

export default slug
